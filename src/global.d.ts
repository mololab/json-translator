import { Sources } from '.';

export {};

declare global {
  var totalTranslation: number;
  var totalTranslated: number;
  var proxyIndex: number;
  var proxyList: string[];
  var source: Sources;
}
